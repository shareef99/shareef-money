node-linker=hoisted
package-import-method=copy
# Fail fast if installing under the wrong Node — better-sqlite3's native binary
# is ABI-specific, and a mismatched Node silently fetched the wrong prebuild.
engine-strict=true
